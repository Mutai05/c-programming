#include <stdio.h>

int main()
{

    // arithmetic operators

    //  + (addition)
    //  - (subtraction)
    //  * (multiplication)
    //  / (division)
    //  % (modulus) Reminder of any division
    //  ++ increment
    //  -- decrement

    //  int z = x + y;
    //  int z = x - y;
    //  int z = x * y;
    //  int z = x / (float) y;

    //  x++;
    //  y--;

    // Take note integer division

    int x = 5;
    int y = 2;

    float z = x / (float)2;

    printf("%f", z);

    return 0;
}